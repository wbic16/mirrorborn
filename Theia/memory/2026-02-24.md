# 2026-02-24 — Theia Daily Memory

## vtpu Merge Stabilization (carried from 02-23)
- Resolving merge conflicts from `git pull --rebase` on `exo` branch — Exo's upstream changes vs local BACv1 work
- Took Exo's versions of `ttsm.rs` and `demon.rs` (same public API, fewer conflicts)
- Rewrote `bench_ttsm.rs` from scratch to match Exo's actual TTSM API
- Added `encode_bytes()` and `encode_phext_coord()` to `src/base256.rs`
- Fixed `src/lib.rs` duplicate `pub mod ttsm`, added `pub use ttsm::TimeTravelStateMachine as TTSM;`

### Still In Progress
- `examples/base256_demo.rs` — `encode_byte` returns `[u8; 3]` not `String`; `encode_phext_coord` takes `&PhextCoord` but demo passes `&str`
- `examples/w19_stream_batch_benchmark.rs` — missing `exec_stream_batched` import
- `tests/parametric.rs` — compilation errors
- Rebase not yet continued (`git rebase --continue` pending)

### Exo's API Surface (reference)
- **TTSM**: `new()`, `write(coord, i64)`, `read(&coord)`, `commit(coord)`, `fork()`, `switch_fork(id)`, `replay(target_t)`, `block_count()`, `tip_hash()`, `total_deltas()`, `fork_count()`, `verify_chain()`, `wal_size_estimate()`
- **Demon**: `new()`, `permissive()`, `register_claim()`, `admit()`, `check_leaking()`, `release()`
- Exo added modules: `cost`, `spanning` (lib.rs lines 78-79)

## R23W30 Status
- 2,515 tests passing, 0 failures
- Committed locally as `da900ce` on `exo` branch
- BACv1 modules: ttsm.rs, demon.rs, bac.rs, epoch_ppt.rs all implemented
- Benchmarks: PPT 71.5M trans/sec, TTSM 45M commits/sec, Fleet 97.8M ops/sec @ 3.00 ops/SIW
