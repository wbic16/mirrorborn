# 2026-02-16 — Chrys 🦋

## Rally Rules Added
- **Rarely remove code without cause.** Refactoring fine. Removing because you don't understand = not fine. A build fix that deletes 2,759 lines should raise alarm.
- **Verse and mirrorborn.us cannot be used for Zen 4 performance testing.** Different architectures — benchmarks would be misleading.
- **Investigate, study, repair, maintain.** Don't rollback. Don't overwrite. Understand what broke and why.

## R23W17-3: Phoenix Re-implemented (commit `ced931f`)
- Rebuilt `phoenix_scheduler.rs` from sibling intent (not blind overwrite)
- `SentronMetrics` built from actual `ExecStats` + `PPTStats` (not phantom fields)
- `CoreMetrics` aggregated from sentron snapshots
- `NineColorDecision`: 9 scores → `harmonic_score()` weighted blend
- `PhoenixScheduler`: evaluate() → Hold or Migrate based on threshold
- Nine colors: ILP(🔴), Core(🟠), SMT(🟡), Cache(🟢), NUMA(🔵), Trend(🟣), Thermal(🟤), Power(⚫), Cluster(⚪)
- Cache weight highest (1.2x) — W17 proved it matters
- 7 new tests, 272 total. Zero deps.

## R23W18: Fix All Warnings (in progress)
- Started cleaning 49 warnings across codebase
- Fixed: unused imports in bitnet, packer, regalloc, scheduler/dag, port_conflicts, cognitive, wedge, integration
- `integration.rs` wrapped with `#[cfg(test)]` in lib.rs (all test code)
- `CognitiveEngine.hd_width` — kept name, added `#[allow(dead_code)]`
- `WedgeExecutor` fields `memory`/`ppt` prefixed with `_`
- `SYS_CLOSE` in perf.rs prefixed with `_`
- Down to ~20 warnings (examples + bins remaining) when session compacted
- 272 tests green throughout

## Key Technical Notes
- `ExecStats` has: `siws_retired`, `ops_retired`, `cycles`, `d_ops`, `s_ops`, `c_ops`, `d_nops`, `s_nops`, `c_nops`
- `ExecStats` does NOT have: `ppt_stats`, `stalls` (sibling code assumed these existed)
- `PPTStats` has `ptc_hit_rate` — use separately
- Port conflicts test code needs `MessageFormat` and `PhextCoord` via `#[cfg(test)]` imports
